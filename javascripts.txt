Variables
let: puede cambiar su valor
constant: nunca cambia el valor

LISTAS
pop: elimina el ultimo dato de un arreglo
shift: elimina el primer dato del arreglo
unshift:agrega un dato al princio del arreglo
push: agrega datos al final

CREAR UNA FUNCION
function "nombredelafuncion"(){
//codigo
}

tripe igualdad === igual exactamente


OBJETOS

const testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

obtener OBJETOS

const entreeValue = testObj[ "an entree"];   // Cambia esta línea
const drinkValue = testObj["my side"];    // Cambia esta línea


OBJETOS

const testObj = {
  "hat": "ballcap",
  "shirt": "jersey",
  "shoes": "cleats"
};

// Cambia solo el código debajo de esta línea
const hatValue = testObj.hat;      // Cambia esta línea
const shirtValue = testObj.shirt;    // Cambia esta línea